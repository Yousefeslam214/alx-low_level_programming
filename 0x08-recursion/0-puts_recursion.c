#include "main.h"

/**
 * _puts_recursion - function that prints a string,
 * followed by a new line.
 * 
 * @s: string
 * 
 * Return: new string.
 */

void _puts_recursion(char *s)
{
	printf("%s\n", s);
}
